<?xml version="1.0"?>
<PluginConfig>
	<Field id="showDebugInfo" type="checkbox">
		<Label>Enable debugging:</Label>
	</Field>

	<Field id="username" type="textfield">
		<Label>Username:</Label>
	</Field>
	<Field id="password" type="textfield" secure="true">
		<Label>Password:</Label>
	</Field>
	<Field id="mfaCode" type="textfield">
		<Label>Multi-factor code:</Label>
	</Field>
	<Field id="l_mfaCode" type="label">
		<Label>Only if your Sense account uses two-factor authentication: enter the current code from your authenticator app. It is used once and then cleared.</Label>
	</Field>
	
	<Field id="rateLimit" type="textfield" defaultValue="60">
		<Label>Poll interval (seconds):</Label>
	</Field>
	<Field id="l_rateLimit" type="label">
		<Label>How often live power is read (30 or more). Daily totals and the appliance list refresh every 5 minutes regardless.</Label>
	</Field>
	<Field id="apiTimeout" type="textfield" defaultValue="30">
		<Label>API timeout (seconds):</Label>
	</Field>

	<Field id="solarEnabled" type="checkbox">
		<Label>Solar enabled:</Label>
	</Field>

	<Field id="csvEnabled" type="checkbox" defaultValue="false">
		<Label>Log power to CSV:</Label>
		<Description>One row per poll in Preferences/Plugins/com.howartp.sense/activeLog-YYYY-MM-DD.csv; 30 days kept.</Description>
	</Field>

	<Field id="folderID" type="textfield">
		<Label>Folder for Sense device creation:</Label>
	</Field>
	
	<Field id="l_folderID" type="label">
		<Label>This should be the ID of the folder, not the name.  Right-click it and "Copy ID".</Label>
	</Field>
	
</PluginConfig>
