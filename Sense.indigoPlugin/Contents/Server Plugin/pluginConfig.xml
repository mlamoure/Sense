<?xml version="1.0"?>
<PluginConfig>
	<Field id="l_account" type="label" fontColor="darkgray">
		<Label>Sense account (home.sense.com). After the first login the plugin keeps Sense's session tokens and renews them; the password is only used again if that fails.</Label>
	</Field>
	<Field id="username" type="textfield">
		<Label>Email:</Label>
	</Field>
	<Field id="password" type="textfield" secure="true">
		<Label>Password:</Label>
	</Field>
	<Field id="mfaCode" type="textfield">
		<Label>Multi-factor code:</Label>
	</Field>
	<Field id="l_mfaCode" type="label" fontSize="small" fontColor="darkgray">
		<Label>Only if your Sense account uses two-factor authentication: the current code from your authenticator app. It is used once and then cleared.</Label>
	</Field>
	<Field id="testLogin" type="button">
		<Label>Check the login:</Label>
		<Title>Test Login</Title>
		<CallbackMethod>testLoginPressed</CallbackMethod>
	</Field>
	<Field id="testLoginResult" type="textfield" readonly="YES">
		<Label>Result:</Label>
	</Field>

	<Field id="sep1" type="separator"/>

	<Field id="rateLimit" type="textfield" defaultValue="60">
		<Label>Poll interval (seconds):</Label>
	</Field>
	<Field id="l_rateLimit" type="label" fontSize="small" fontColor="darkgray">
		<Label>How often live power is read (30 or more). Today's kWh and the appliance list refresh every 5 minutes regardless.</Label>
	</Field>
	<Field id="apiTimeout" type="textfield" defaultValue="30">
		<Label>API timeout (seconds):</Label>
	</Field>

	<Field id="folderID" type="menu">
		<Label>Folder for Sense devices:</Label>
		<List class="self" method="deviceFolderList" dynamicReload="true"/>
	</Field>
	<Field id="l_folderID" type="label" fontSize="small" fontColor="darkgray">
		<Label>New appliance devices are created here. Existing devices are never moved.</Label>
	</Field>

	<Field id="solarEnabled" type="checkbox">
		<Label>Solar enabled:</Label>
		<Description>Also create and track the Sense "Solar" device.</Description>
	</Field>
	<Field id="csvEnabled" type="checkbox" defaultValue="false">
		<Label>Log power to CSV:</Label>
		<Description>One row per poll in Preferences/Plugins/com.howartp.sense/activeLog-YYYY-MM-DD.csv; 30 days kept.</Description>
	</Field>

	<Field id="sep2" type="separator"/>

	<Field id="showDebugInfo" type="checkbox">
		<Label>Debug logging:</Label>
		<Description>Show debug detail in the Event Log. The plugin's own log file always keeps debug detail.</Description>
	</Field>
</PluginConfig>
